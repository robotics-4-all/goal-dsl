import complex_goals.goal as complex_goals;
import topic_goals.goal as topic_goals;
import time_constraints.goal as tc;


Sequence S1 -> {
    timeConstraint: tc.TC1;
    addGoal(complex_goals.GoalC);
    addGoal(complex_goals.GoalD);
    addGoal(complex_goals.GoalE);
}

Sequence S2 -> {
    addGoal(topic_goals.TopicGoalA);
    addGoal(topic_goals.TopicGoalB);
    addGoal(topic_goals.TopicGoalC);
    addGoal(topic_goals.TopicGoalD);
}
